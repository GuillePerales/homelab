APPDATA=/mnt/vault/apps/vaultwarden/data
TZ=Europe/Madrid
CONTAINER_IP=10.0.30.2X   # rango docker-main: .26–.41
DOMAIN=homelab.local

# Generar con: openssl rand -base64 48
ADMIN_TOKEN=

# SMTP (opcional — para invitaciones y 2FA email)
SMTP_HOST=
SMTP_FROM=
SMTP_USER=
SMTP_PASS=
